<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agriculture.mapper.MenuMapper">
<!--    <select id="selectMtitleByUid" resultType="java.lang.String">-->
<!--        select distinct m.mtitle-->
<!--        from user_role ur-->
<!--                 left join role r on r.rid = ur.rid-->
<!--                 left join administrate a  on r.rid = a.rid-->
<!--                 left join menu m on m.mid = a.mid-->
<!--        where ur.uid=#{uid}-->
<!--          and m.state=0-->
<!--          and r.state=0-->
<!--    </select>-->

</mapper>
